
<div class="nav-bar">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home') }}"><img src="https://static.vecteezy.com/system/resources/thumbnails/004/699/321/small/rh-or-hr-letter-logo-isolated-on-white-background-vector.jpg" height="150" width="150"></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% if app.user is null %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Me connecter</a>
                        </li>
                    {% endif %}
                    {% if app.user is not null %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_user_index') }}">Liste des salariés</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                        </li>
                    {% endif %}
                    {% if app.user is not null and is_granted('ROLE_RH') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_user_new') }}">Ajouter du personnel</a>
                        </li>
                    {% endif %}
                </ul>

            </div>
        </div>
    </nav>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
    {% endfor %}
</div>